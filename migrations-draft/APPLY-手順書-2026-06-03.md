# 2026-06-03 LINE本番反映 適用手順書（v3.7 新軸）

> ⚠️ この手順は **こっさん / line-ops が手動で実行** します。
> Claude（下書き担当）は wrangler 実行・D1書き込み・worker deploy・git push・
> プロセス操作を一切していません。本番には触れていません。

## 前提

- 対象repo: `占い事業/line-harness-oss`（子repo・branch `kossan-custom`）
- 投入SQL: `migrations-draft/v3.7-new-axis-2026-06-03.sql`
- workerコード差し替え草稿: `migrations-draft/buildGreetingMessages-new-axis.ts.md`
- 実行者: こっさん（wrangler認証・Worker API認証が必要）
- 新軸の正本: `占い事業/LINE/ステップ配信v3.md`（v3.7）

## 対象データ

- friend_add scenario_id = `8ee12685-8ffa-491d-8bed-0c788a02307f`（step 0,1,3,4,5,6,7）
- 納品後フォロー scenario_id = `post_purchase_followup_v1`（step 1〜6）

`post_purchase_followup_v1` が存在しない場合、納品後フォローのUPSERTは0件です（v3.4と同仕様）。先にscenario作成方針を確認してください。

## 事前確認

```bash
cd 占い事業/line-harness-oss

# 対象DB名を確認
grep -n "database_name\|binding" apps/worker/wrangler.toml

# 対象scenarioの存在確認
npx wrangler d1 execute <db-name> --remote --command "SELECT id, name FROM scenarios WHERE id IN ('8ee12685-8ffa-491d-8bed-0c788a02307f', 'post_purchase_followup_v1');"
```

## 1. D1ダンプバックアップ（必須・最初に取る）

```bash
cd 占い事業/line-harness-oss
mkdir -p _backup

npx wrangler d1 export <db-name> \
  --output=_backup/d1-backup-2026-06-03-before-v3.7.sql \
  --remote

# サイズが0でないこと
ls -lh _backup/d1-backup-2026-06-03-before-v3.7.sql
```

## 2. ローカル投入テスト（構文・冪等性の確認）

```bash
npx wrangler d1 execute <db-name> \
  --file=migrations-draft/v3.7-new-axis-2026-06-03.sql \
  --local

# 再実行してUPDATE/ON CONFLICTが失敗しないこと（冪等）
npx wrangler d1 execute <db-name> \
  --file=migrations-draft/v3.7-new-axis-2026-06-03.sql \
  --local
```

## 3. 本番投入（新seed適用）

```bash
npx wrangler d1 execute <db-name> \
  --file=migrations-draft/v3.7-new-axis-2026-06-03.sql \
  --remote
```

## 4. 検証SELECT

```bash
# 本文プレビュー
npx wrangler d1 execute <db-name> --remote --command "SELECT scenario_id, step_order, delay_minutes, substr(message_content, 1, 80) AS preview FROM scenario_steps WHERE (scenario_id = '8ee12685-8ffa-491d-8bed-0c788a02307f' AND step_order IN (0,1,3,4,5,6,7)) OR (scenario_id = 'post_purchase_followup_v1' AND step_order IN (1,2,3,4,5,6)) ORDER BY scenario_id, step_order;"

# 旧軸キーワード残存チェック（0件であること）
npx wrangler d1 execute <db-name> --remote --command "SELECT scenario_id, step_order FROM scenario_steps WHERE scenario_id IN ('8ee12685-8ffa-491d-8bed-0c788a02307f','post_purchase_followup_v1') AND (message_content LIKE '%彼の気持ちタロット%' OR message_content LIKE '%彼の気持ちの温度感%' OR message_content LIKE '%彼の気持ちを当てる%' OR message_content LIKE '%32歳%' OR message_content LIKE '%33歳%' OR message_content LIKE '%婚活疲れ%' OR message_content LIKE '%結婚の話が出ない%' OR message_content LIKE '%縁の絡まり%' OR message_content LIKE '%眠り縁%');"
```

`v3.7-new-axis-2026-06-03.sql` 末尾にも同じ検証SELECTが入っているので、`--file` 実行時の標準出力でも確認できます。

## 5. workerコード差し替え → deploy

`buildGreetingMessages-new-axis.ts.md` の「置換後コード」を
`apps/worker/src/routes/webhook.ts` の `buildGreetingMessages()` 本体へ反映します。

- 関数シグネチャ・import・`export { webhook };` は変更しない
- 3吹き出し（flex画像 / text2 / text3）構造は維持

```bash
pnpm install
pnpm build   # 型エラーなく通ること

npx wrangler deploy
```

## 6. 友だち追加E2E（テストアカウント）

テストLINEアカウントで確認します。

- 友だち追加直後のあいさつ3吹き出し（画像→届くもの→Day0）が新軸で届く
  - 「その違和感は、気のせいじゃない」「映すのは彼の心ではなくあなたの直感」が出る
  - 旧軸（32歳/33歳・結婚の話が出ない・彼の気持ちを当てる）が**出ない**
- Day1以降のstep配信予約が新軸文面で入る
- Day5/6/7 のCTAが MOSH（`mosh.jp/services/354707`）へ遷移する
- 納品後フォローscenarioがある場合、step 1〜6 の新軸文面

## 7. ロールバック

最優先はバックアップから復元します。

```bash
npx wrangler d1 execute <db-name> \
  --file=_backup/d1-backup-2026-06-03-before-v3.7.sql \
  --remote
```

workerコードを戻す場合は、git で webhook.ts を直前コミットへ戻して再deploy:

```bash
cd 占い事業/line-harness-oss
git checkout -- apps/worker/src/routes/webhook.ts   # 未コミット変更を破棄する場合
# もしくは直前コミットの版へ
git checkout HEAD -- apps/worker/src/routes/webhook.ts
pnpm build
npx wrangler deploy
```

## 8. commit順（子repo → 親gitlink）

`.claude/rules/branches.md` に従う。**逆順は他PCで展開不能**。

```bash
# 1) 子repo内でcommit / push
cd 占い事業/line-harness-oss
git checkout kossan-custom
git add migrations-draft/ apps/worker/src/routes/webhook.ts
git commit -m "feat(line): v3.7 新軸（裏がある関係/違和感の肯定）seed + 友だち追加挨拶"
git push origin kossan-custom

# 2) 親repoに戻ってgitlink更新commit / push
cd ../../
git add 占い事業/line-harness-oss
git commit -m "chore(uranai): bump line-harness-oss gitlink (v3.7 new axis)"
git push
```

## 既知の注意点

- `apps/worker/src/services/diagnosis-bot.ts` の診断Bot文面は今回の対象外（2026-05-24方針で無料診断Botは廃止済。残存していれば別タスク）。
- Day5 ¥480 ミニのCTAは `mosh.jp/services/TODO_MINI_480` プレースホルダー。MOSH ¥480ページ作成後にサービスIDを差し替える（正本: ステップ配信v3.md Day5 注記）。
- v3.4 の step_order 2（friend_add）は今回触らない（v3.4でも対象外）。
